const assert = require('assert');
const zealousFeynman = require('../index.js');

describe('zealousFeynman', () => {
  it('has a test', () => {
    assert(false, 'zealousFeynman should have a test');
  });
});
